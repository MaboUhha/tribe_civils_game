# Tribe Civils Game

**Симулятор племён глубокой древности** — гибрид Dwarf Fortress × WorldBox × Civilization

## 📋 Содержание

- [Обзор проекта](#обзор-проекта)
- [Архитектура](#архитектура)
- [Игровые системы](#игровые-системы)
- [Управление](#управление)
- [Технологии](#технологии)
- [Разработка](#разработка)

---

## Обзор проекта

Tribe Civils Game — это симулятор глубокой древности, где игрок начинает управлять небольшим племенем и наблюдает за развитием цивилизации.

### Ключевые особенности

| Характеристика | Значение |
|----------------|----------|
| Максимум племён | 500 |
| Размер племени | 10–500 человек |
| Размер карты | 200×150 тайлов |
| Тип игры | Real-time с паузой |
| Графика | ASCII/символьная (Canvas) |
| Платформа | PWA (веб-приложение) |

### Концепция

Игрок начинает с одним племенем в процедурно сгенерированном мире. По мере роста населения, освоения технологий и расширения территории племя превращается в прото-государство.

---

## Архитектура

### Структура проекта

```
src/
├── core/              # Ядро симуляции (чистая логика)
│   ├── constants.ts   # Константы игры
│   ├── world.ts       # Генерация мира и карта
│   ├── tribe.ts       # Логика племени
│   ├── tech.ts        # Дерево технологий
│   └── events.ts      # Генератор событий
│
├── game/              # Игровая логика
│   └── state.ts       # Состояние игры, игровой цикл
│
├── storage/           # Работа с данными
│   └── db.ts          # IndexedDB обёртки
│
├── ui/                # Пользовательский интерфейс
│   ├── map.ts         # Рендеринг карты на Canvas
│   └── panels.ts      # UI панели (статы, события)
│
├── types.ts           # Общие типы TypeScript
└── main.ts            # Точка входа
```

### Модули ядра

#### `core/world.ts` — Генерация мира

Использует **Perlin-подобный шум** для создания естественного ландшафта:

1. **Генерация высот** — 4 октавы шума для рельефа
2. **Генерация влажности** — 3 октавы для биомов
3. **Сглаживание** — 2 прохода усреднения для плавных переходов
4. **Определение биомов** — по комбинации высоты и влажности:
   - Вода (elevation < 0.3)
   - Песок (0.3–0.35)
   - Трава/Лес (0.35–0.6, зависит от влажности)
   - Холмы (0.6–0.8)
   - Горы (> 0.8)
5. **Спавн ресурсов** — вероятность зависит от типа местности

#### `core/tribe.ts` — Логика племени

**Состояние племени:**
- `population` — численность (10–500)
- `resources` — запасы (еда, дерево, камень, металл)
- `position` — позиция на карте
- `state` — состояние (кочевое, оседлое, война, союз)
- `relations` — отношения с другими племенами (-100…100)
- `discoveredTechs` — изученные технологии

**Ежедневный тик:**
```typescript
tick():
  1. Потребление еды (population × 1 единица)
  2. Проверка на голод (смерть при нехватке еды)
  3. Рождаемость (шанс при избытке еды)
  4. Смертность (естественная)
  5. Действие ИИ (сбор ресурсов, перемещение)
```

**Действия:**
- `gatherResources()` — сбор ресурсов с тайла
- `explore()` — перемещение в поисках ресурсов
- `move()` — перемещение по направлению
- `settle()` — основание поселения (требуется 30+ человек)

#### `core/tech.ts` — Дерево технологий

**3 эпохи, 13 технологий:**

| Эпоха | Технологии |
|-------|------------|
| **Каменный век** | Простые инструменты, Огонь, Копьё, Плетение корзин, Укрытие |
| **Бронзовый век** | Земледелие, Гончарство, Добыча камня, Колесо |
| **Железный век** | Обработка бронзы, Письменность, Обработка железа |

**Эффекты технологий:**
- `GATHER_RATE` — скорость сбора ресурсов
- `BIRTH_RATE` — рождаемость
- `MOVE_SPEED` — скорость перемещения
- `STORAGE` — вместимость хранилищ
- `COMBAT` — боевая мощь

#### `core/events.ts` — Генератор событий

**Типы событий:**

| Событие | Шанс | Приоритет | Описание |
|---------|------|-----------|----------|
| Удачный сбор | 20% | 5 | +бонус еды |
| Болезнь | 15% | 7 | -10% населения |
| Открытие | 15% | 6 | Новые земли |
| Прирост населения | 15% | 5 | +15% населения |
| Засуха | 15% | 7 | -30% еды |
| Рейд | 10% | 8 | Нападение другого племени |
| Предложение союза | 5% | 6 | Другое племя предлагает союз |
| Объявлена война | 5% | 9 | Враждебное племя объявляет войну |

**События с выбором:**
- Рейд: защититься или откупиться
- Союз: принять или отклонить

---

## Игровые системы

### Демография

```typescript
Константы:
  BIRTH_RATE = 0.002      // Шанс рождения в тик
  DEATH_RATE = 0.001      // Шанс смерти в тик
  STARVATION_RATE = 0.005 // Шанс смерти от голода
  FOOD_CONSUMPTION = 1    // Еды на человека в тик
```

**Формулы:**
- Потребление: `population × FOOD_CONSUMPTION`
- Рождения: `floor(population × BIRTH_RATE)` при `food > 50`
- Смерти: `floor(population × DEATH_RATE)`
- Голод: `floor(population × STARVATION_RATE)` при `food = 0`

### Экономика

**Ресурсы:**
- 🍖 **Еда** — собирается с травы, леса, болот; нужна для выживания
- 🪵 **Дерево** — собирается в лесу, на болотах; нужно для технологий
- 🪨 **Камень** — добывается в холмах, горах; нужно для построек
- ⚙️ **Металл** — добывается в горах; нужен для продвинутых технологий

**Сбор ресурсов:**
```typescript
gatherAmount = min(resourceAmount, floor(population × 0.5))
```

### Отношения между племенами

**Шкала отношений:** `-100 … +100`

| Диапазон | Статус | Реакция |
|----------|--------|---------|
| < -70 | Война | Нападение, рейды |
| -70 … -20 | Вражда | Избегание, конфликты |
| -20 … 20 | Нейтралитет | Случайные взаимодействия |
| 20 … 50 | Дружба | Торговля, помощь |
| > 50 | Союз | Совместные действия |

**Изменение отношений:**
- Торговля: +5
- Принятие союза: +30
- Отклонение союза: -10
- Откуп при рейде: +10
- Рейд: -10

### Игровой цикл

```
┌─────────────────────────────────────┐
│           Game Loop                 │
│                                     │
│  ┌─────────────┐    ┌────────────┐  │
│  │   update()  │───▶│  render()  │  │
│  │   (tick)    │    │  (Canvas)  │  │
│  └─────────────┘    └────────────┘  │
│         │                  │         │
│         ▼                  ▼         │
│  ┌─────────────┐    ┌────────────┐  │
│  │   events()  │    │    UI()    │  │
│  └─────────────┘    └────────────┘  │
└─────────────────────────────────────┘
```

**Скорости игры:**
- `PAUSED` (0) — игра на паузе
- `NORMAL` (1) — 1 тик/сек
- `FAST` (2) — 2 тика/сек
- `ULTRA` (3) — 3 тика/сек

---

## Управление

### Клавиатура

| Клавиша | Действие |
|---------|----------|
| `W` / `↑` | Камера вверх |
| `S` / `↓` | Камера вниз |
| `A` / `←` | Камера влево |
| `D` / `→` | Камера вправо |
| `+` / `=` | Увеличить зум |
| `-` / `_` | Уменьшить зум |
| `Пробел` | Пауза / Старт |
| `F` | Показать сетку |
| `R` | Показать ресурсы |

### Мышь

| Действие | Результат |
|----------|-----------|
| Клик по тайлу | Выбрать племя на тайле |

### UI панели

**Верхняя панель:**
- Тик игры
- Население игрока
- Количество племён
- Индикатор скорости
- Кнопки: Пауза, Сохранить, Загрузить

**Боковая панель (выбрано племя):**
- Информация о племени (название, население, состояние, позиция)
- Ресурсы племени
- Кнопки управления (движение, поселение, сбор)
- Доступные технологии
- Отношения с другими племенами

**Панель событий (низ):**
- Лог событий с временными метками
- Приоритетные события выделяются красным

---

## Технологии

### Стек

| Компонент | Технология | Назначение |
|-----------|------------|------------|
| Язык | **TypeScript 5.6** | Типизация, рефакторинг |
| Сборка | **Vite 6.0** | Быстрая сборка, HMR |
| Фронтенд | **Canvas API** | Рендеринг карты |
| Хранение | **IndexedDB** | Сохранения, настройки |
| PWA | **Workbox** | Оффлайн, кэширование |

### Зависимости

```json
{
  "dependencies": {
    "idb": "^8.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-plugin-pwa": "^0.21.0",
    "workbox-window": "^7.3.0"
  }
}
```

### PWA возможности

- **Оффлайн-режим** — работа без интернета
- **Установка** — как нативное приложение
- **Автосохранение** — каждые 100 тиков
- **Кэширование** — ассеты и код

---

## Разработка

### Требования

- Node.js 18+
- Yarn 1.22+

### Установка

```bash
yarn install
```

### Запуск разработки

```bash
yarn dev
```

Сервер: http://localhost:3000

### Сборка

```bash
yarn build
```

Результат в папке `dist/`

### Предварительный просмотр

```bash
yarn preview
```

### Линтинг

```bash
yarn lint
```

---

## Планы развития

### Ближайшие обновления

- [ ] Боевая система (осады, сражения)
- [ ] Здания и улучшения территории
- [ ] Торговые пути между племенами
- [ ] Динамические названия племён
- [ ] Система героев/лидеров
- [ ] Религия и культура
- [ ] Стихийные бедствия

### Долгосрочные цели

- [ ] Мультиплеер (WebRTC)
- [ ] Моддинг (JSON-конфиги)
- [ ] Сценарии и достижения
- [ ] Экспорт/импорт сохранений
- [ ] Мобильная адаптация

---

## Лицензия

MIT

---

## Контакты

Репозиторий: `C:\projects\hobby\games\tribe_civils_game`
